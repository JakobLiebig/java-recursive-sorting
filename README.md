# java-recursive-sorting
A few recursive implementations of sorting algorithms as preperation for a uni exam.

```sh
git clone git@github.com:JakobLiebig/java-recursive-sorting.git
cd java-recursive-sorting
gradle run
```
